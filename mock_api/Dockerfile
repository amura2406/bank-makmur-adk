FROM python:3.11-slim
WORKDIR /code
COPY requirements.txt /code/requirements.txt
RUN pip install --no-cache-dir --upgrade -r /code/requirements.txt
COPY . /code/mock_api
ENV PYTHONPATH=/code
CMD ["sh", "-c", "uvicorn mock_api.main:app --host 0.0.0.0 --port ${PORT:-8000}"]
